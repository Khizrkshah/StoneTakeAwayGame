package model;

/**
 * The enum Class for the Square property used in the GameState Class.
 */
public enum Square {
    /**
     * Used to make the image in the Square Visible.
     */
    VISIBLE,
    /**
     * Used to make the image in the Square Hidden.
     */
    HIDDEN
}
